import type { Metadata } from "next";
import { Inter, JetBrains_Mono } from "next/font/google";
import "./globals.css";

const inter = Inter({
  subsets: ["latin"],
  variable: "--font-inter",
});

const jetbrainsMono = JetBrains_Mono({
  subsets: ["latin"],
  variable: "--font-jetbrains-mono",
});

export const metadata: Metadata = {
  title: "GUHANIA RESTAURANTGROUP LLC | Restaurant Management & Software Solutions",
  description: "Leading restaurant group that manages dining establishments and develops cutting-edge software tools including POS systems, inventory management, RushMyFood delivery platform, and AI-driven analytics.",
  keywords: "restaurant management, POS systems, RushMyFood, inventory management, restaurant software, data analytics, AI marketing",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${inter.variable} ${jetbrainsMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
